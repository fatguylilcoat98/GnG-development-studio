# Runtime state is re-seeded/accumulated at runtime, never version-controlled.
state/

# Generated founder/status reports — regenerate with scripts/founder-report.sh
reports/*.md

# Python / OS cruft
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# Safe Server Agent local config — server-specific paths, never committed.
# Copy agent_config.example.json to agent_config.json and edit.
agent_config.json
